export const elements = {
  UPLOAD_BUTTON: document.querySelector('input[type="file"]')
}

export const apiEndpoints = {
  DEPLOY_PREVIEW: '/api/deploy-preview',
  DEPLOY_PRODUCTION: '/api/deploy-production'
}
